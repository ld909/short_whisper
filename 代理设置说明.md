# Whisper模型代理设置说明

## 概述
为了加速OpenAI Whisper模型的下载，我已经在您的脚本中添加了自动代理设置功能。

## 代理配置
根据您的截图，脚本会自动尝试以下代理端口（按优先级排序）：

1. **混合代理端口**: 7897 （首选）
2. **HTTP(S)代理端口**: 7899
3. **SOCKS代理端口**: 7898

## 工作原理
1. 脚本启动时会自动测试各个代理端口的连通性
2. 选择第一个可用的代理端口进行配置
3. 设置相应的环境变量（HTTP_PROXY, HTTPS_PROXY等）
4. 如果所有代理都不可用，会回退到直连模式

## 修改的文件
- `buda/mp3tofineEngsrt.py` - 主控制脚本
- `buda/mp3toscripts.py` - Whisper转录模块
- `test_proxy.py` - 代理测试脚本

## 使用方法

### 1. 测试代理连接
```bash
python test_proxy.py
```

### 2. 运行主程序
```bash
python buda/mp3tofineEngsrt.py <主题名称>
```

例如：
```bash
python buda/mp3tofineEngsrt.py code
```

## 输出示例
程序启动时会显示类似以下信息：
```
尝试使用混合代理端口 7897...
✓ 混合代理连接成功！
已设置代理: http://127.0.0.1:7897
代理设置完成，Whisper模型下载将通过代理进行
```

## 故障排除

### 如果代理连接失败
1. 确保您的代理软件正在运行
2. 检查代理端口是否正确开启
3. 验证防火墙设置

### 如果仍然下载缓慢
1. 尝试重启代理软件
2. 检查代理软件的网络连接
3. 可以手动修改脚本中的代理端口配置

## 手动配置代理端口
如果需要使用其他端口，可以修改脚本中的 `proxy_configs` 列表：

```python
proxy_configs = [
    {"name": "自定义代理", "port": "您的端口号"},
    {"name": "混合代理", "port": "7897"},
    # ... 其他配置
]
```

## 注意事项
- 代理设置只影响模型下载，不影响本地音频文件处理
- 首次运行时会下载Whisper模型，后续运行会使用缓存的模型
- 建议在GPU环境下运行以获得最佳性能 